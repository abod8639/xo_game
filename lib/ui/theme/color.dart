//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = const Color.fromARGB(255, 0, 0, 0);
  static Color secondaryColor = const Color(0xFF001456);
  static Color accentColor = const Color.fromARGB(255, 0, 60, 255);
}
